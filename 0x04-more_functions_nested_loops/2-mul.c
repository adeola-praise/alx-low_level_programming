#include "main.h"

/**
 * mul - Multiplies two integers.
 *
 * @a: first integer.
 * @b: second integer.
 *
 * Return: the result of multiplying a and b.
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
